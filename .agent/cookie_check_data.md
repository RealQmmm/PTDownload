# Cookie 周期性检查任务详解

## 概述
系统会定期检查 PT 站点的 Cookie 有效性，同时**顺便抓取用户统计数据**，实现一举两得的效果。

---

## 定时任务配置

### 触发时机
**文件**: `server/src/services/schedulerService.js:38-62`

```javascript
startCookieCheckJob() {
    // 从设置中读取间隔（默认60分钟）
    const interval = parseInt(setting?.value || '60');
    
    // 定时任务：每 N 分钟执行一次
    this.cookieJob = schedule.scheduleJob(`*/${interval} * * * *`, async () => {
        const results = await siteService.checkAllCookies();
        // 记录日志
    });
}
```

**默认频率**: 每 **60 分钟**（可在设置中修改 `cookie_check_interval`）

---

## 数据抓取流程

### 完整调用链
```
schedulerService.startCookieCheckJob()  (每60分钟)
    ↓
siteService.checkAllCookies()  (遍历所有启用的站点)
    ↓
siteService.checkCookie(siteId)  (单个站点检查)
    ↓
1. 访问站点首页
2. 检查 Cookie 是否有效
3. 如果有效 → 解析用户统计数据
4. 更新数据库
```

---

## 抓取的数据详解

### 1. Cookie 有效性检查

**文件**: `server/src/services/siteService.js:123-194`

#### 检查方法
```javascript
// 访问站点首页
const response = await axios.get(site.url, {
    headers: {
        'Cookie': site.cookies,
        'User-Agent': 'Mozilla/5.0...'
    }
});

// 检查是否被重定向到登录页
const isLogin = html.includes('login.php') ||
                html.includes('id="login"') ||
                html.includes('登录') && !html.includes('退出') ||
                html.includes('Login') && !html.includes('Logout');

// 判断结果
const status = isLogin ? 1 : 0;  // 0=有效, 1=失效
```

#### 更新字段
- `cookie_status`: 0 (有效) 或 1 (失效)
- `last_checked_at`: 检查时间戳

---

### 2. 用户统计数据（Cookie 有效时）

**文件**: `server/src/utils/siteParsers.js:237-273`

如果 Cookie 有效，会调用 `parseUserStats()` 解析以下数据：

#### 📊 抓取的数据字段

| 字段 | 说明 | 示例 | 正则表达式 |
|------|------|------|------------|
| **username** | 用户名 | `qinming` | `<a href*="userdetails.php">` |
| **upload** | 上传量 | `12.5 TB` | `/(上传量\|Uploaded)[:\s]+([\d.]+\s*[MGT]B)/i` |
| **download** | 下载量 | `2.3 TB` | `/(下载量\|Downloaded)[:\s]+([\d.]+\s*[MGT]B)/i` |
| **ratio** | 分享率 | `5.43` | `/(分享率\|Ratio)[:\s]+([\d.]+)/i` |
| **bonus** | 魔力值/积分 | `15,204` | `/(魔力值\|Bonus\|积分\|Karma)[:\s]+([\d,.]+)/i` |
| **level** | 用户等级 | `精英用户` | `/(等级\|Class\|Level)[:\s]+([^\s]+)/i` |
| **isCheckedIn** | 是否已签到 | `true/false` | 检测文本中的签到关键词 |

#### 代码实现
**文件**: `server/src/utils/siteParsers.js:241-270`

```javascript
const parseUserStats = (html, type) => {
    if (type === 'NexusPHP') {
        const $ = cheerio.load(html);
        const stats = {};
        
        // 1. 用户名
        const userLink = $('a[href*="userdetails.php"]').first();
        stats.username = userLink.text().trim();
        
        // 2. 分享率
        const text = $('body').text();
        const ratioMatch = text.match(/(分享率|Ratio)[:\s]+([\d.]+)/i);
        if (ratioMatch) stats.ratio = ratioMatch[2];
        
        // 3. 上传量
        const uploadMatch = text.match(/(上传量|Uploaded)[:\s]+([\d.]+\s*[MGT]B)/i);
        if (uploadMatch) stats.upload = uploadMatch[2];
        
        // 4. 下载量
        const downloadMatch = text.match(/(下载量|Downloaded)[:\s]+([\d.]+\s*[MGT]B)/i);
        if (downloadMatch) stats.download = downloadMatch[2];
        
        // 5. 魔力值/积分
        const bonusMatch = text.match(/(魔力值|Bonus|积分|Karma)[:\s]+([\d,.]+)/i);
        if (bonusMatch) stats.bonus = bonusMatch[2];
        
        // 6. 用户等级
        const levelMatch = text.match(/(等级|Class|Level)[:\s]+([^\s]+)/i);
        if (levelMatch) stats.level = levelMatch[2];
        
        // 7. 签到状态
        const alreadyCheckedIn = text.includes('已经签到') ||
                                 text.includes('今日已签到') ||
                                 text.includes('签到成功');
        stats.isCheckedIn = alreadyCheckedIn;
        
        return stats;
    }
};
```

---

### 3. 数据库更新

**文件**: `server/src/services/siteService.js:153-176`

#### 更新的数据库字段

```sql
UPDATE sites SET 
    cookie_status = 0,           -- Cookie 有效
    last_checked_at = NOW(),     -- 检查时间
    username = ?,                -- 用户名
    upload = ?,                  -- 上传量
    download = ?,                -- 下载量
    ratio = ?,                   -- 分享率
    bonus = ?,                   -- 魔力值
    level = ?,                   -- 用户等级
    stats_updated_at = NOW()     -- 统计更新时间
WHERE id = ?;
```

#### 代码实现
```javascript
if (status === 0) {  // Cookie 有效
    const stats = siteParsers.parseUserStats(html, site.type);
    
    if (stats) {
        // 更新热力图数据（如果上传量增加）
        this._updateHeatmapData(id, site.upload, stats.upload);
        
        // 更新站点统计
        db.prepare(`
            UPDATE sites SET 
                cookie_status = 0, 
                last_checked_at = ?,
                username = ?, 
                upload = ?, 
                download = ?, 
                ratio = ?, 
                bonus = ?, 
                level = ?, 
                stats_updated_at = ?
            WHERE id = ?
        `).run(now, stats.username, stats.upload, stats.download, 
               stats.ratio, stats.bonus, stats.level, now, id);
    }
}
```

---

### 4. 热力图数据（上传量增量）

**文件**: `server/src/services/siteService.js:102-121`

#### 功能说明
如果检测到上传量增加，会自动更新 `site_daily_stats` 表，用于生成热力图。

#### 计算逻辑
```javascript
_updateHeatmapData(siteId, currentUploadStr, newUploadStr) {
    // 1. 解析字节数
    const currentBytes = parseSizeToBytes(currentUploadStr);  // 旧值: 12.5 TB
    const newBytes = parseSizeToBytes(newUploadStr);          // 新值: 12.8 TB
    
    // 2. 计算增量
    if (newBytes > currentBytes && currentBytes > 0) {
        const delta = newBytes - currentBytes;  // 0.3 TB
        const today = '2026-01-02';
        
        // 3. 更新或插入今日统计
        db.prepare(`
            INSERT INTO site_daily_stats (site_id, date, uploaded_bytes)
            VALUES (?, ?, ?)
            ON CONFLICT(site_id, date) DO UPDATE SET
                uploaded_bytes = uploaded_bytes + ?
        `).run(siteId, today, delta, delta);
    }
}
```

#### 数据表结构
```sql
CREATE TABLE site_daily_stats (
    site_id INTEGER,
    date TEXT,                    -- YYYY-MM-DD
    uploaded_bytes INTEGER,       -- 当日上传增量（字节）
    PRIMARY KEY (site_id, date)
);
```

---

## 完整数据流图

```
┌─────────────────────────────────────────────────────────┐
│  定时任务 (每60分钟)                                      │
│  schedulerService.startCookieCheckJob()                 │
└────────────┬────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│  遍历所有启用的站点                                        │
│  siteService.checkAllCookies()                          │
└────────────┬────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│  单个站点检查                                             │
│  siteService.checkCookie(siteId)                        │
│                                                         │
│  1. 访问站点首页 (GET site.url)                          │
│     带上 Cookie                                          │
│                                                         │
│  2. 检查 HTML 内容                                       │
│     ├─ 包含 "login.php" → Cookie 失效                    │
│     └─ 包含 "退出/Logout" → Cookie 有效                  │
└────────────┬────────────────────────────────────────────┘
             │
             ├─ Cookie 失效 ────────────────────────────┐
             │                                          │
             │  ┌───────────────────────────────────┐  │
             │  │ 更新数据库:                        │  │
             │  │ - cookie_status = 1 (失效)        │  │
             │  │ - last_checked_at = NOW()         │  │
             │  │                                   │  │
             │  │ 发送通知:                          │  │
             │  │ - notifyCookieExpiration()        │  │
             │  │ - 记录错误日志                     │  │
             │  └───────────────────────────────────┘  │
             │                                          │
             └─ Cookie 有效 ────────────────────────────┤
                                                        │
                ┌───────────────────────────────────────▼──┐
                │  解析用户统计数据                          │
                │  siteParsers.parseUserStats(html)        │
                │                                           │
                │  抓取字段:                                 │
                │  ✓ username   (用户名)                    │
                │  ✓ upload     (上传量: 12.5 TB)           │
                │  ✓ download   (下载量: 2.3 TB)            │
                │  ✓ ratio      (分享率: 5.43)              │
                │  ✓ bonus      (魔力值: 15,204)            │
                │  ✓ level      (等级: 精英用户)             │
                │  ✓ isCheckedIn (签到状态)                 │
                └───────────────┬───────────────────────────┘
                                │
                                ▼
                ┌───────────────────────────────────────────┐
                │  更新数据库 (sites 表)                     │
                │                                           │
                │  UPDATE sites SET                         │
                │    cookie_status = 0,                     │
                │    last_checked_at = NOW(),               │
                │    username = 'qinming',                  │
                │    upload = '12.5 TB',                    │
                │    download = '2.3 TB',                   │
                │    ratio = '5.43',                        │
                │    bonus = '15,204',                      │
                │    level = '精英用户',                     │
                │    stats_updated_at = NOW()               │
                │  WHERE id = ?                             │
                └───────────────┬───────────────────────────┘
                                │
                                ▼
                ┌───────────────────────────────────────────┐
                │  更新热力图数据 (site_daily_stats 表)       │
                │                                           │
                │  IF 上传量增加:                            │
                │    旧值: 12.5 TB                          │
                │    新值: 12.8 TB                          │
                │    增量: 0.3 TB                           │
                │                                           │
                │  INSERT OR UPDATE site_daily_stats        │
                │    (site_id, date, uploaded_bytes)        │
                │  VALUES (1, '2026-01-02', 322122547200)   │
                └───────────────────────────────────────────┘
```

---

## 数据用途

### 1. Cookie 状态监控
- **前端显示**: 设置页面显示 Cookie 状态（✅ 有效 / ❌ 失效）
- **过期提醒**: Cookie 失效时发送通知
- **自动处理**: 失效后可提示用户更新 Cookie

### 2. 用户统计展示
- **个人数据**: 显示各站点的上传量、下载量、分享率等
- **数据对比**: 对比不同站点的数据
- **趋势分析**: 查看数据变化趋势

### 3. 热力图可视化
- **每日上传**: 显示每个站点每天的上传增量
- **贡献统计**: 可视化对各站点的贡献
- **时间范围**: 默认显示最近 90 天

---

## 关键代码位置

| 功能 | 文件 | 行数 |
|------|------|------|
| 定时任务启动 | `schedulerService.js` | 38-62 |
| Cookie 检查 | `siteService.js` | 123-194 |
| 数据解析 | `siteParsers.js` | 237-273 |
| 热力图更新 | `siteService.js` | 102-121 |
| 数据库更新 | `siteService.js` | 162-172 |

---

## 配置说明

### 修改检查频率
**位置**: 设置页面 → `cookie_check_interval`

```javascript
// 默认值: 60 分钟
// 可选范围: 1-1440 分钟 (1分钟 - 24小时)
```

### 支持的站点类型
- ✅ **NexusPHP**: 大部分中文 PT 站点
- ✅ **Mock**: 测试用模拟站点
- ⚠️ 其他类型需要添加对应的解析器

---

## 总结

Cookie 周期性检查任务**一举两得**:

1. **主要功能**: 检查 Cookie 是否有效
   - 防止 Cookie 过期影响自动化任务
   - 及时通知用户更新 Cookie

2. **附加功能**: 抓取用户统计数据
   - ✅ 用户名、上传量、下载量
   - ✅ 分享率、魔力值、等级
   - ✅ 签到状态
   - ✅ 每日上传增量（热力图）

3. **效率优势**:
   - 无需额外请求，一次访问获取所有数据
   - 减少对 PT 站点的访问频率
   - 数据实时性高（每小时更新）

**数据准确性**: 依赖于 PT 站点的 HTML 结构，使用正则表达式和 CSS 选择器解析，对主流 NexusPHP 站点兼容性好。
