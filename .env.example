# PTDownload 环境变量配置示例

# ============================================
# 数据库配置
# ============================================

# 是否使用外部挂载的数据库
# true: 使用外部数据库（适合数据持久化和迁移）
# false: 使用内置数据库（默认，数据存储在 ./data 目录）
USE_EXTERNAL_DB=false

# 外部数据库路径（仅当 USE_EXTERNAL_DB=true 时生效）
# 这是容器内的路径，需要在 docker-compose.yml 中配置对应的 volume 挂载
# 例如：如果你的数据库文件在宿主机的 /home/user/ptdb/ptdownload.db
# 你需要在 docker-compose.yml 中添加：
#   volumes:
#     - /home/user/ptdb:/external_db
# 然后设置：
EXTERNAL_DB_PATH=/external_db/ptdownload.db

# ============================================
# 应用配置
# ============================================

# 应用端口
PORT=3000

# 时区设置
TZ=Asia/Shanghai

# Node 环境
NODE_ENV=production
