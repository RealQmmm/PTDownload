# PT Download Manager (PT下载管理助手)

这是一个基于 Docker 部署的 PT (Private Tracker) 下载管理工具，旨在提供统一的界面来管理多个 PT 站点的搜索、聚合以及下载任务。

## ✨ 主要功能

*   **聚合搜索**: 支持同时搜索多个 PT 站点的资源，按种子数排序，快速找到高质量资源。
*   **下载管理**: 集成主流下载客户端（目前支持 Transmission、QBittorrent 等），实时查看下载进度和任务状态。
*   **一键下载**: 搜索结果可直接推送到下载器。
*   **站点管理**: 管理 PT 站点配置（Cookie, URL），支持 NexusPHP 架构的站点。
*   **数据统计**: 下载量、上传量统计。
*   **现代化界面**: 基于 React 和 TailwindCSS 构建的响应式深色模式界面。

## 🛠️ 技术栈

### 前端 (Client)
*   **框架**: React 18
*   **构建工具**: Vite
*   **样式**: TailwindCSS
*   **路由**: React Router (推测，或简单的条件渲染)
*   **HTTP请求**: Axios (推测)

### 后端 (Server)
*   **运行时**: Node.js
*   **Web框架**: Express
*   **数据库**: SQLite (Better-SQLite3)
*   **爬虫/解析**: Cheerio, Axios
*   **定时任务**: Node-Schedule

### 部署
*   **容器化**: Docker, Docker Compose

## 🚀 快速开始 (使用 Docker)

### 前置要求
*   安装 [Docker](https://www.docker.com/) 和 [Docker Compose](https://docs.docker.com/compose/)。

### 部署步骤

1.  **克隆项目**
    ```bash
    git clone <your-repo-url>
    cd PTDownload
    ```

2.  **启动服务**
    ```bash
    docker-compose up -d
    ```

3.  **访问应用**
    打开浏览器访问 `http://localhost:3000`。

## 📦 开发指南

### 目录结构
```
PTDownload/
├── client/                 # 前端代码 (React)
├── server/                 # 后端代码 (Express)
├── data/                   # SQLite 数据库文件 (Docker volume)
├── Dockerfile              # 多阶段构建文件
└── docker-compose.yml      # Docker Compose 配置
```

### 本地开发

**后端开发**
```bash
cd server
npm install
npm run dev
```

**前端开发**
```bash
cd client
npm install
npm run dev
```

## 📝 环境变量

| 变量名 | 默认值 | 描述 |
| ------ | ------ | ---- |
| PORT | 3000 | 服务端口 |
| DB_PATH | ./data/ptdownload.db | SQLite 数据库路径 |

## 🤝 贡献
欢迎提交 Pull Request 或 Issue！

## 📄 许可证
MIT License
